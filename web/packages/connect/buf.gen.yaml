version: v2
clean: true
plugins:
  - local: ./node_modules/.bin/protoc-gen-es
    out: src/gen
    opt:
      - target=ts
      - import_extension=js
inputs:
  # Single source of truth: the same proto the Rust crate compiles.
  - directory: ../../../crates/connectrpc/proto
